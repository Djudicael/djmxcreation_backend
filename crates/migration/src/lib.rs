//! Placeholder crate.
//!
//! Database migrations are applied from `crates/repository/src/config/db.rs` via
//! embedded Refinery migrations. This crate intentionally stays empty so the
//! workspace keeps a single migration execution path.
